window.events = {
    879: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
        }
    },
    880: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
        }
    },
    900: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
        }
    },
    901: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
        }
    },
    989: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
        }
    },
    1036: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
        }
    },
    1037: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
        }
    },
    1243: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
        }
    },
    1244: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
        }
    },
    1291: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
        }
    },
    1292: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
        }
    },
    1305: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
        }
    },
    1306: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
        }
    },
    1313: {
        onInsertion: function($scope) {
            if (++$scope.options.strOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [STR] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.strOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [STR] orbs support.' });
        }
    },
    1314: {
        onInsertion: function($scope) {
            if (++$scope.options.strOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [STR] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.strOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [STR] orbs support.' });
        }
    },
    1472: {
        onInsertion: function($scope) {
            if (++$scope.options.rainbowOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RAINBOW] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.rainbowOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RAINBOW] orbs support.' });
        }
    },
    1473: {
        onInsertion: function($scope) {
            if (++$scope.options.rainbowOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RAINBOW] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.rainbowOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RAINBOW] orbs support.' });
        }
    },
    1523: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
        }
    },
    1524: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
        }
    },
    1531: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    1532: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    1609: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    1610: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    1651: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    1652: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    1901: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
        }
    },
    1902: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
        }
    },
    1940: {
        onInsertion: function($scope) {
            if (++$scope.options.strOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [STR] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.strOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [STR] orbs support.' });
        }
    },
    1941: {
        onInsertion: function($scope) {
            if (++$scope.options.strOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [STR] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.strOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [STR] orbs support.' });
        }
    },
    2012: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    2013: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    2040: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled == 0)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
        }
    },
    2041: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled == 0)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
        }
    },
    2108: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled == 0)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
        }
    },
    2109: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1){}
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] and [SEMLA] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled == 0){}
            if (--$scope.options.meatOrbsEnabled == 0)
                $scope.notify({ text: 'Enabling [G] and [SEMLA] orbs support.' });
        }
    },
    2232: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    2233: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    2234: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    2241: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
        }
    },
    2242: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
        }
    },
    2500: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    2373: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    2374: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    2375: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    2476: {
        onInsertion: function($scope) {
            if (++$scope.options.dexOrbsEnabled == 1){}
            if (++$scope.options.intOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [DEX] and [INT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.dexOrbsEnabled === 0){}
            if (--$scope.options.intOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [DEX] and [INT] orbs support.' });
        }
    },
    2477: {
        onInsertion: function($scope) {
            if (++$scope.options.dexOrbsEnabled == 1){}
            if (++$scope.options.intOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [DEX] and [INT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.dexOrbsEnabled === 0){}
            if (--$scope.options.intOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [DEX] and [INT] orbs support.' });
        }
    },
    2525: {
        onInsertion: function($scope) {
            if (++$scope.options.intOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [INT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.intOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [INT] orbs support.' });
        }
    },
    2631: {
        onInsertion: function($scope) {
            if (++$scope.options.rainbowOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RAINBOW] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.rainbowOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RAINBOW] orbs support.' });
        }
    },
    2776: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    2801: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    2802: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    2803: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    2804: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    2957: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    2958: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    2979: {
        onInsertion: function($scope) {
            if (++$scope.options.superBombOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [SUPERBOMB] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.superBombOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [SUPERBOMB] orbs support.' });
        }
    },
    2980: {
        onInsertion: function($scope) {
            if (++$scope.options.superBombOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [SUPERBOMB] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.superBombOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [SUPERBOMB] orbs support.' });
        }
    },
    3008: {
        onInsertion: function($scope) {
            if (++$scope.options.sugarToysEnabled == 1)
                $scope.notify({ text: 'Enabling Hobby-Hobby Transformation support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.sugarToysEnabled === 0)
                $scope.notify({ text: 'Disabling Hobby-Hobby Transformation support.' });
        }
    },
    3009: {
        onInsertion: function($scope) {
            if (++$scope.options.sugarToysEnabled == 1)
                $scope.notify({ text: 'Enabling Hobby-Hobby Transformation support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.sugarToysEnabled === 0)
                $scope.notify({ text: 'Disabling Hobby-Hobby Transformation support.' });
        }
    },
    3174: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3175: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3306: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RCV] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RCV] orbs support.' });
        }
    },
    3307: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RCV] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RCV] orbs support.' });
        }
    },
    3350: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3389: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3390: {
        onInsertion: function($scope) {
            if (++$scope.options.tokiStateEnabled == 1)
                $scope.notify({ text: 'Enabling Toki-Toki Effect support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.tokiStateEnabled === 0)
                $scope.notify({ text: 'Disabling Toki-Toki Effect support.' });
        }
    },
    3391: {
        onInsertion: function($scope) {
            if (++$scope.options.tokiStateEnabled == 1)
                $scope.notify({ text: 'Enabling Toki-Toki Effect support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.tokiStateEnabled === 0)
                $scope.notify({ text: 'Disabling Toki-Toki Effect support.' });
        }
    },
    3429: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3430: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3519: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3522: {
        onInsertion: function($scope) {
            if (++$scope.options.cloneEnabled == 1)
                $scope.notify({ text: 'Enabling Clone-Clone support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.cloneEnabled === 0)
                $scope.notify({ text: 'Disabling Clone-Clone support.' });
        }
    },
    3523: {
        onInsertion: function($scope) {
            if (++$scope.options.cloneEnabled == 1)
                $scope.notify({ text: 'Enabling Clone-Clone support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.cloneEnabled === 0)
                $scope.notify({ text: 'Disabling Clone-Clone support.' });
        }
    },
    3560: {
        onInsertion: function($scope) {
            if (++$scope.options.strOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [STR] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.strOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [STR] orbs support.' });
        }
    },
    3562: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3563: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3596: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    3597: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    3608: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3609: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3675: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3676: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3722: {
        onInsertion: function($scope) {
            if (++$scope.options.tokiStateEnabled == 1)
                $scope.notify({ text: 'Enabling Selected Character Effect support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.tokiStateEnabled === 0)
                $scope.notify({ text: 'Disabling Selected Character Effect support.' });
        }
    },
    3723: {
        onInsertion: function($scope) {
            if (++$scope.options.tokiStateEnabled == 1)
                $scope.notify({ text: 'Enabling Toki-Toki Effect support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.tokiStateEnabled === 0)
                $scope.notify({ text: 'Disabling Selected Character Effect support.' });
        }
    },
    3740: {
        onInsertion: function($scope) {
            if (++$scope.options.emptyOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [EMPTY] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.emptyOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [EMPTY] orbs support.' });
        }
    },
    3741: {
        onInsertion: function($scope) {
            if (++$scope.options.emptyOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [EMPTY] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.emptyOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [EMPTY] orbs support.' });
        }
    },
    3744: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3745: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3762: {
        onInsertion: function($scope) {
            if (++$scope.options.superBombOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [SUPERBOMB] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.superBombOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [SUPERBOMB] orbs support.' });
        }
    },
    3805: {
        onInsertion: function($scope) {
            if (++$scope.options.sugarToysEnabled == 1)
                $scope.notify({ text: 'Enabling Hobby-Hobby Transformation support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.sugarToysEnabled === 0)
                $scope.notify({ text: 'Disabling Hobby-Hobby Transformation support.' });
        }
    },
    3814: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1){}
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RCV] and [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0){}
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RCV] and [WANO] orbs support.' });
        }
    },
    3881: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3883: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3888: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1){}
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RCV] and [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0){}
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RCV] and [WANO] orbs support.' });
        }
    },
    3889: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1){}
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RCV] and [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0){}
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RCV] and [WANO] orbs support.' });
        }
    },
    3890: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1){}
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RCV] and [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0){}
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RCV] and [WANO] orbs support.' });
        }
    },
    3891: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1){}
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RCV] and [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0){}
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RCV] and [WANO] orbs support.' });
        }
    },
    3895: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1){}
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] and [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0){}
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] and [WANO] orbs support.' });
        }
    },
    3896: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1){}
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] and [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0){}
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] and [WANO] orbs support.' });
        }
    },
    3897: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1){}
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] and [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0){}
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] and [WANO] orbs support.' });
        }
    },
    3898: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1){}
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] and [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0){}
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] and [WANO] orbs support.' });
        }
    },
    3899: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3900: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3903: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3904: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1){}
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] and [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0){}
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] and [MEAT] orbs support.' });
        }
    },
    3905: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1){}
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] and [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0){}
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] and [MEAT] orbs support.' });
        }
    },
    3943: {
        onInsertion: function($scope) {
            if (++$scope.options.rainbowOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RAINBOW] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.rainbowOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RAINBOW] orbs support.' });
        }
    },
    3944: {
        onInsertion: function($scope) {
            if (++$scope.options.rainbowOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RAINBOW] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.rainbowOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RAINBOW] orbs support.' });
        }
    },
    3947: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1){}
            if (++$scope.options.meatOrbsEnabled == 1){}
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO], [TND] and [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0){}
            if (--$scope.options.meatOrbsEnabled === 0){}
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO], [TND] and [MEAT] orbs support.' });
        }
    },
    3948: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1){}
            if (++$scope.options.meatOrbsEnabled == 1){}
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO], [TND] and [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0){}
            if (--$scope.options.meatOrbsEnabled === 0){}
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO], [TND] and [MEAT] orbs support.' });
        }
    },
    3950: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1){}
            if (++$scope.options.meatOrbsEnabled == 1){}
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO], [TND] and [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0){}
            if (--$scope.options.meatOrbsEnabled === 0){}
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO], [TND] and [MEAT] orbs support.' });
        }
    },
    3953: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3954: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3955: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RCV] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RCV] orbs support.' });
        }
    },
    3956: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RCV] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RCV] orbs support.' });
        }
    },
    3957: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RCV] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RCV] orbs support.' });
        }
    },
    3958: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3959: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3962: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3963: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3964: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3966: {
        onInsertion: function($scope) {
            if (++$scope.options.tndOrbsEnabled == 1){}
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RCV] and [TND] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.tndOrbsEnabled === 0){}
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RCV] and [TND] orbs support.' });
        }
    },
    3967: {
        onInsertion: function($scope) {
            if (++$scope.options.tndOrbsEnabled == 1){}
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RCV] and [TND] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.tndOrbsEnabled === 0){}
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RCV] and [TND] orbs support.' });
        }
    },
    3972: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1){}
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] and [SEMLA] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled == 0){}
            if (--$scope.options.meatOrbsEnabled == 0)
                $scope.notify({ text: 'Enabling [WANO] and [SEMLA] orbs support.' });
        }
    },
    3973: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1){}
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] and [SEMLA] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled == 0){}
            if (--$scope.options.meatOrbsEnabled == 0)
                $scope.notify({ text: 'Enabling [WANO] and [SEMLA] orbs support.' });
        }
    },
    3983: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3984: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3985: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3986: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3987: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3988: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    3992: {
        onInsertion: function($scope) {
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] orbs support.' });
        }
    },
    3993: {
        onInsertion: function($scope) {
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] orbs support.' });
        }
    },
    3996: {
        onInsertion: function($scope) {
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] orbs support.' });
        }
    },
    4028: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] orbs support.' });
            if (++$scope.options.rainbowOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RAINBOW] orbs support.' });
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] orbs support.' });
            if (--$scope.options.rainbowOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RAINBOW] orbs support.' });
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    4029: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] orbs support.' });
            if (++$scope.options.rainbowOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RAINBOW] orbs support.' });
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] orbs support.' });
            if (--$scope.options.rainbowOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RAINBOW] orbs support.' });
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    4038: {
        onInsertion: function($scope) {
            if (++$scope.options.superBombOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [SUPERBOMB] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.superBombOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [SUPERBOMB] orbs support.' });
        }
    },
    4039: {
        onInsertion: function($scope) {
            if (++$scope.options.superBombOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [SUPERBOMB] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.superBombOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [SUPERBOMB] orbs support.' });
        }
    },
    4042: {
        onInsertion: function($scope) {
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] orbs support.' });
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] orbs support.' });
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    4043: {
        onInsertion: function($scope) {
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] orbs support.' });
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] orbs support.' });
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    4044: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5052: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    5053: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    5054: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    5055: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    5056: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    5057: {
        onInsertion: function($scope) {
            if (++$scope.options.meatOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [MEAT] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.meatOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [MEAT] orbs support.' });
        }
    },
    5168: {
        onInsertion: function($scope) {
            if (++$scope.options.rainbowOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RAINBOW] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.rainbowOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RAINBOW] orbs support.' });
        }
    },
    5169: {
        onInsertion: function($scope) {
            if (++$scope.options.rainbowOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RAINBOW] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.rainbowOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RAINBOW] orbs support.' });
        }
    },
    5170: {
        onInsertion: function($scope) {
            if (++$scope.options.rainbowOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RAINBOW] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.rainbowOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RAINBOW] orbs support.' });
        }
    },
    5171: {
        onInsertion: function($scope) {
            if (++$scope.options.rainbowOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RAINBOW] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.rainbowOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RAINBOW] orbs support.' });
        }
    },
    5172: {
        onInsertion: function($scope) {
            if (++$scope.options.rainbowOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RAINBOW] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.rainbowOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RAINBOW] orbs support.' });
        }
    },
    5173: {
        onInsertion: function($scope) {
            if (++$scope.options.rainbowOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RAINBOW] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.rainbowOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RAINBOW] orbs support.' });
        }
    },
    5174: {
        onInsertion: function($scope) {
            if (++$scope.options.rainbowOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RAINBOW] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.rainbowOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RAINBOW] orbs support.' });
        }
    },
    5175: {
        onInsertion: function($scope) {
            if (++$scope.options.rainbowOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RAINBOW] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.rainbowOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RAINBOW] orbs support.' });
        }
    },
    5176: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5177: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5178: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5179: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5180: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5181: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5182: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5183: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5202: {
        onInsertion: function($scope) {
            if (++$scope.options.emptyOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [EMPTY] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.emptyOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [EMPTY] orbs support.' });
        }
    },
    5203: {
        onInsertion: function($scope) {
            if (++$scope.options.emptyOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [EMPTY] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.emptyOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [EMPTY] orbs support.' });
        }
    },
    5204: {
        onInsertion: function($scope) {
            if (++$scope.options.emptyOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [EMPTY] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.emptyOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [EMPTY] orbs support.' });
        }
    },
    5205: {
        onInsertion: function($scope) {
            if (++$scope.options.emptyOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [EMPTY] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.emptyOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [EMPTY] orbs support.' });
        }
    },
    5206: {
        onInsertion: function($scope) {
            if (++$scope.options.emptyOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [EMPTY] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.emptyOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [EMPTY] orbs support.' });
        }
    },
    5207: {
        onInsertion: function($scope) {
            if (++$scope.options.emptyOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [EMPTY] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.emptyOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [EMPTY] orbs support.' });
        }
    },
    5208: {
        onInsertion: function($scope) {
            if (++$scope.options.emptyOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [EMPTY] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.emptyOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [EMPTY] orbs support.' });
        }
    },
    5209: {
        onInsertion: function($scope) {
            if (++$scope.options.emptyOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [EMPTY] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.emptyOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [EMPTY] orbs support.' });
        }
    },
    5291: {
        onInsertion: function($scope) {
            if (++$scope.options.rainbowOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RAINBOW] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.rainbowOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RAINBOW] orbs support.' });
        }
    },
    5292: {
        onInsertion: function($scope) {
            if (++$scope.options.rainbowOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RAINBOW] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.rainbowOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RAINBOW] orbs support.' });
        }
    },
    5293: {
        onInsertion: function($scope) {
            if (++$scope.options.rainbowOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RAINBOW] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.rainbowOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RAINBOW] orbs support.' });
        }
    },
    5294: {
        onInsertion: function($scope) {
            if (++$scope.options.rainbowOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RAINBOW] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.rainbowOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RAINBOW] orbs support.' });
        }
    },
    5295: {
        onInsertion: function($scope) {
            if (++$scope.options.rainbowOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RAINBOW] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.rainbowOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RAINBOW] orbs support.' });
        }
    },
    5296: {
        onInsertion: function($scope) {
            if (++$scope.options.rainbowOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RAINBOW] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.rainbowOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RAINBOW] orbs support.' });
        }
    },
    5297: {
        onInsertion: function($scope) {
            if (++$scope.options.rainbowOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RAINBOW] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.rainbowOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RAINBOW] orbs support.' });
        }
    },
    5298: {
        onInsertion: function($scope) {
            if (++$scope.options.rainbowOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [RAINBOW] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.rainbowOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [RAINBOW] orbs support.' });
        }
    },
    5397: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5398: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5399: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5400: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5401: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5402: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5403: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5404: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5429: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5430: {
        onInsertion: function($scope) {
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] orbs support.' });
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] orbs support.' });
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5431: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5432: {
        onInsertion: function($scope) {
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] orbs support.' });
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] orbs support.' });
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5453: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5454: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5455: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5456: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5457: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5458: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5459: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5460: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5461: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5462: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5463: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5464: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5465: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5466: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5467: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5468: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5469: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5470: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5471: {
        onInsertion: function($scope) {
            if (++$scope.options.wanoOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [WANO] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.wanoOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [WANO] orbs support.' });
        }
    },
    5520: {
        onInsertion: function($scope) {
            if (++$scope.options.emptyOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [EMPTY] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.emptyOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [EMPTY] orbs support.' });
        }
    },
    5521: {
        onInsertion: function($scope) {
            if (++$scope.options.emptyOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [EMPTY] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.emptyOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [EMPTY] orbs support.' });
        }
    },
    5522: {
        onInsertion: function($scope) {
            if (++$scope.options.emptyOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [EMPTY] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.emptyOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [EMPTY] orbs support.' });
        }
    },
    5523: {
        onInsertion: function($scope) {
            if (++$scope.options.emptyOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [EMPTY] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.emptyOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [EMPTY] orbs support.' });
        }
    },
    5539: {
        onInsertion: function($scope) {
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] orbs support.' });
        }
    },
    5540: {
        onInsertion: function($scope) {
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] orbs support.' });
        }
    },
    5541: {
        onInsertion: function($scope) {
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] orbs support.' });
        }
    },
    5542: {
        onInsertion: function($scope) {
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] orbs support.' });
        }
    },
    5543: {
        onInsertion: function($scope) {
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] orbs support.' });
        }
    },
    5544: {
        onInsertion: function($scope) {
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] orbs support.' });
        }
    },
    5545: {
        onInsertion: function($scope) {
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] orbs support.' });
        }
    },
    5546: {
        onInsertion: function($scope) {
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] orbs support.' });
        }
    },
    5547: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] orbs support.' });
        }
    },
    5548: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] orbs support.' });
        }
    },
    5549: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] orbs support.' });
        }
    },
    5550: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] orbs support.' });
        }
    },
    5551: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] orbs support.' });
        }
    },
    5552: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] orbs support.' });
        }
    },
    5553: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] orbs support.' });
        }
    },
    5554: {
        onInsertion: function($scope) {
            if (++$scope.options.gOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [G] orbs support.' });
            if (++$scope.options.tndOrbsEnabled == 1)
                $scope.notify({ text: 'Enabling [TND] orbs support.' });
        },
        onRemoval: function($scope) {
            if (--$scope.options.gOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [G] orbs support.' });
            if (--$scope.options.tndOrbsEnabled === 0)
                $scope.notify({ text: 'Disabling [TND] orbs support.' });
        }
    },
};
